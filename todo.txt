- Dale’s comments
	- refactor duplicate parts in checkSaveBeforeCompile and closeTab, probably
- restoring behavior where tab header text color changes when file has unsaved changes (I was trying using an EventFilter on the tab pane, but it colored all of the tabs instead of just the one)
